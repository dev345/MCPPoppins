# MCPoppins low-fidelity wireframes

## Home

```text
[sticky nav: mark | Marketplace Workflows Docs Pricing Trust | CTA]
[hero copy / CTA / trust strip] [live Slack·Teams·Telegram agent demo]
[channel portability strip]
[three product principles]
[channels] -> [identity + policy + audit] -> [Jira / Confluence]
[featured package cards]
[three-step competition workflow]
[large closing CTA]
```

## Marketplace

```text
[page title + trust-led copy]
[search] [status filters]
[result count / no-sign-in note]
[responsive package-card grid]
```

## Reusable package detail

```text
[package mark + summary + CTAs] [status/version/posture/source]
[sticky section navigation]
[tool + risk list]                       [works-with / compatibility]
[permission table]                       [prototype boundary]
[install tabs + three setup steps]
[example prompts]
[trust evidence]
```

## Installation guide

```text
[in-page section rail] [mode cards]
                       [command block + no-secrets callout]
                       [channel adapter sequence]
                       [sample doctor result]
                       [first portable prompt]
```

## Responsive rule

At 720px, every core layout becomes one column, tables collapse into labelled rows, navigation becomes compact, and generated agent UI remains a narrow-safe card rather than a dashboard.
